import React from 'react'

// components
import Layout from '../components/layout'
import Seo from '../components/seo'

const Schedule = () => (
  <Layout>
    <Seo title='Schedule' />

    <p style={{color: '#fff'}}>Schedule page</p>
  </Layout>
)

export default Schedule